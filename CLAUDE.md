# Ulala 개발 가이드

이 문서는 AI 어시스턴트가 Ulala 프로젝트에서 개발 작업을 수행할 때 따라야 하는 가이드라인입니다.

## 가장 중요한 원칙

어떤 파일이든 함부로 삭제하지 마세요. 만약 삭제가 필요하다면 꼭 개발자에게 물어보세요.

## 🎨 디자인 원칙

**모든 개발 작업에서 `docs/design-guide.md`를 필수적으로 참고하여 일관된 디자인을 적용해야 합니다.**

### 핵심 디자인 요구사항

- Microsoft Fluent 2 디자인 시스템 기반
- Tailwind CSS v4 커스텀 테마 사용
- 일관성(Consistency), 가독성(Readability), 단순성(Simplicity) 원칙 준수
- 라이트/다크 모드 지원 필수
- 모바일 우선 반응형 디자인
- WCAG AA 접근성 기준(4.5:1 색상 대비) 준수

## 🔄 개발 프로세스

### 1단계: 기능 문서 작성

새로운 기능 개발이나 기존 기능 개선 시, 다음 절차를 따릅니다:

1. **기능 문서 생성**: `feature/` 디렉토리에 기능 명세서 작성

   ```
   feature/
   ├── [기능명].md           # 새로운 기능 명세서
   ```

2. **문서 구조**:

   ```markdown
   # [기능명] 기능 명세서

   ## 개요

   - 기능의 목적과 필요성

   ## 요구사항

   - 기능적 요구사항
   - 비기능적 요구사항

   ## 디자인 고려사항

   - docs/design-guide.md의 어떤 원칙을 적용할지
   - 사용할 컴포넌트와 색상 시스템

   ## 구현 계획

   - 단계별 구현 방법
   - 필요한 파일 및 컴포넌트

   ## 테스트 계획

   - 테스트 시나리오
   - 검증 방법
   ```

3. **문서 작성 원칙**:
   - **구체적인 코드 예시를 작성하지 않는다**
   - 기능의 목적, 요구사항, 디자인 방향만 명시
   - 구현 계획은 "무엇을 할 것인가"에 집중, "어떻게 코드를 작성할 것인가"는 구현 단계에서 결정
   - 코드 블록은 타입 정의나 데이터 구조 예시 정도로만 제한

### 2단계: 개발자 승인

1. 기능 문서 작성 완료 후 개발자에게 검토 요청
2. 개발자가 문서를 확인하고 승인할 때까지 대기
3. 승인 후에만 실제 코드 구현 시작

### 3단계: 구현

1. **디자인 가이드 준수**: `docs/design-guide.md` 의 모든 원칙 적용
2. **코드 작성**: 승인된 명세서에 따라 구현
3. **테스트 실행**: 필요시 테스트코드 작성 또는 개선, 기능 동작 확인
4. **린트/타입체크**: 코드 품질 검증

### 4단계: 문서 업데이트

구현 완료 후 다음 작업 수행:

1. **기능 문서 업데이트**: 실제 구현된 내용을 반영
   - 구현 시 변경된 사항 기록
   - 추가된 컴포넌트나 파일 목록 업데이트
   - 발견된 이슈나 개선점 기록
   - 개선 관련된 문서는 기존 문서에 요약하여 병합, 이 때 중복이 있다면 제거

2. **완료 상태 표시**: 문서 상단에 구현 완료 표시

   ```markdown
   # [기능명] 기능 명세서

   **상태**: ✅ 구현 완료 (YYYY-MM-DD)
   **구현된 파일**: [파일 목록]
   **주요 변경사항**: [변경사항 요약]
   ```

## 🎯 개발 시 필수 적용 리스트

### 디자인 적용 요구사항

- `docs/design-guide.md`의 색상 시스템을 반드시 적용하라
- Tailwind CSS v4 커스텀 테마를 사용하라
- 5가지 버튼 상태를 모두 구현하라 (기본/hover/active/disabled/loading)
- 모바일 우선 반응형 디자인을 적용하라
- 라이트/다크 모드를 지원하라
- WCAG AA 접근성 기준(4.5:1)을 준수하라
- 8px 기반 간격 시스템을 사용하라

### 코드 품질 요구사항

- TypeScript 타입을 정의하라
- ESLint 규칙을 준수하라
- 컴포넌트 재사용성을 고려하라
- 성능을 최적화하라 (애니메이션 150-300ms)
- 에러 처리를 구현하라
- 테스트 코드를 작성하라

### 문서화 요구사항

- 기능 명세서를 작성하라
- 개발자 승인을 확인하라
- 구현 후 문서를 업데이트하라
- 코드 주석을 작성하라 (필요시)

## 🚫 금지사항

1. **승인 없는 구현**: 기능 문서 작성 없이 바로 코드 구현 금지
2. **디자인 가이드 무시**: `docs/design-guide.md` 원칙을 따르지 않는 구현 금지
3. **문서 업데이트 누락**: 구현 후 관련 문서 업데이트 누락 금지
4. **접근성 무시**: 키보드 접근성, 스크린 리더 지원 누락 금지
5. **성능 저해**: 불필요한 애니메이션이나 무거운 구현 금지

## Import 경로 규칙
- **절대 경로 사용**: `@/` 별칭을 사용하여 `app/` 디렉토리의 절대 경로로 import
- **상대 경로 금지**: `./` 또는 `../`를 사용한 상대 경로 import 금지

## 📝 로깅(Logging) 규칙

### 기본 원칙

**모든 로그는 환경별 로거 유틸리티(`app/utils/logger.ts`)를 사용하며, 직접 `console.*` 메서드를 사용하지 않습니다.**

### 이유

- **프로덕션 환경 보호**: 개발 중 디버깅 로그가 프로덕션에 노출되지 않도록 방지
- **보안**: 민감한 정보가 프로덕션 콘솔에 노출되는 것을 방지
- **일관성**: 중앙화된 로깅 시스템으로 향후 에러 모니터링 서비스(Sentry 등) 연동 용이
- **성능**: 불필요한 로그 출력으로 인한 프로덕션 성능 저하 방지

### 사용 방법

#### ❌ 잘못된 방법 (금지)
```typescript
// console 직접 사용
console.log("사용자 로그인 성공");
console.error("API 요청 실패", error);
console.warn("토큰이 만료되었습니다");
```

#### ✅ 올바른 방법
```typescript
// logger 유틸리티 사용
import { logger } from "@/utils/logger";

logger.log("사용자 로그인 성공");
logger.error("API 요청 실패", error);
logger.warn("토큰이 만료되었습니다");
```

### Logger API

`app/utils/logger.ts`는 다음 메서드를 제공합니다:

- `logger.error(...)`: 에러 로그 (개발 환경에서만 출력, 향후 Sentry 등 연동)
- `logger.warn(...)`: 경고 로그 (개발 환경에서만 출력)
- `logger.info(...)`: 정보 로그 (개발 환경에서만 출력)
- `logger.debug(...)`: 디버그 로그 (개발 환경에서만 출력)
- `logger.log(...)`: 일반 로그 (개발 환경에서만 출력)

### 동작 방식

- **개발 환경** (`import.meta.env.DEV === true`): 모든 로그가 콘솔에 출력됨
- **프로덕션 환경** (`import.meta.env.DEV === false`): 로그가 출력되지 않음

### 필수 사항

- 모든 로그 출력 시 `logger` 유틸리티를 사용
- `console.log`, `console.error`, `console.warn` 등 직접 사용 금지
- 새로운 코드 작성 시 logger 사용 필수
- 기존 코드 수정 시 console 호출을 logger로 변경

## 📡 API 엔드포인트 관리 규칙

### 기본 원칙

**모든 API 엔드포인트는 중앙에서 관리하며, 컴포넌트에서 직접 URL을 작성하지 않습니다.**

### 파일 구조

```
app/
└── api/
    ├── api.ts           # API 설정 및 클라이언트 함수
    └── endpoints.ts     # API 엔드포인트 상수 정의
```

### 구현 규칙

1. **API 설정 및 클라이언트** (`app/api/api.ts`)
   - 환경 변수(`VITE_API_BASE_URL`)는 이 파일에서만 읽어옴
   - BASE_URL과 기타 API 관련 설정 관리
   - fetch API를 래핑한 클라이언트 함수 제공 (GET, POST, PUT, DELETE)
   - 인증 토큰 자동 주입 옵션
   - 표준화된 에러 핸들링 및 타임아웃 처리

2. **엔드포인트 상수 정의** (`app/api/endpoints.ts`)
   - 모든 API 엔드포인트 경로를 상수로 정의
   - 카테고리별로 객체로 구조화 (예: AUTH, MEMBERS, TODOS 등)
   - TypeScript의 `as const`를 사용하여 타입 안전성 보장

### 사용 예시

#### ❌ 잘못된 방법 (금지)
```typescript
// 컴포넌트에서 직접 URL 작성
const API_BASE_URL = import.meta.env.VITE_API_BASE_URL;
const response = await fetch(`${API_BASE_URL}/api/auth/token`, {
  method: "POST",
});
```

#### ✅ 올바른 방법
```typescript
// 1. api 폴더에서 import
import { API_ENDPOINTS } from "@/api/endpoints";
import { apiClient } from "@/api/api";

// 2. 타입 안전한 API 호출
const response = await apiClient.post(API_ENDPOINTS.AUTH.TOKEN);
```

### 필수 사항

- 새로운 API 엔드포인트 추가 시 반드시 `app/api/endpoints.ts`에 먼저 정의
- 컴포넌트에서 `import.meta.env.VITE_API_BASE_URL`을 직접 사용 금지
- 문자열 리터럴로 API URL 작성 금지
- API 호출 시 `apiClient` 유틸리티 함수 사용 권장

## 🔧 커맨드 참고

### 자주 사용하는 명령어

```bash
# 개발 서버 실행
npm run dev

# 린트 검사
npm run lint

# 타입 체크
npm run typecheck

# 테스트 실행
npm run test

# 빌드
npm run build
```

이 가이드를 따라 일관성 있고 품질 높은 코드를 작성하고, 사용자 경험을 향상시키는 기능을 구현해주세요.
